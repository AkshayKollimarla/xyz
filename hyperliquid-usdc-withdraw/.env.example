########################################
# HYPERLIQUID USDC WITHDRAW - ENVIRONMENT
# Copy this file to `.env` and fill in real values THERE, not here.
# This file (.env.example) IS committed to git — never put a real private
# key or address in it. `.env` is gitignored; that's where real values go.
########################################

# Private key of your Hyperliquid MASTER account (the same key behind the
# MetaMask wallet you use to log into Hyperliquid).
# Withdrawals MUST be signed by the master account key — an API/agent wallet
# (the kind trading bots normally use) is not permitted to withdraw funds.
PRIVATE_KEY=0xyourmasteraccountprivatekeyhere

# Optional: private key of a second Hyperliquid master account. If set, its
# balance and positions are shown alongside the first account's, and the
# "Flat All & Withdraw Everything" action covers both accounts. Leave blank
# to manage only one account.
PRIVATE_KEY_2=

# Default destination address for withdrawals (Arbitrum). This can be ANY
# address, not necessarily one you control — Hyperliquid's withdraw action
# lets you specify an arbitrary destination directly. Both accounts (if
# PRIVATE_KEY_2 is set) withdraw to this same address.
# You can also override this per-withdrawal in the UI.
DESTINATION_ADDRESS=0xYourDestinationAddressHere

# Local server port.
PORT=3001

# The app binds to this host. Defaults to 127.0.0.1 (only reachable via a
# reverse proxy on the same machine, e.g. Caddy). Set to 0.0.0.0 to make
# the app itself directly reachable over the network (e.g. IP:port access
# with no reverse proxy) — only do this deliberately, and see
# COOKIE_SECURE below, since plain HTTP access needs it set to false.
BIND_HOST=127.0.0.1

# Optional: comma-separated names of HIP-3 perp dexes your account holds
# balance in (shown as "Perps (<name>)" in the Hyperliquid frontend, e.g.
# "xyz"). The status view displays them, and every withdrawal automatically
# sweeps their withdrawable balance into the main Perps dex first — so a
# blank-amount withdrawal really does move your entire account.
# Leave blank if you only use the main Perps balance.
EXTRA_PERP_DEXES=

# Optional: Arbitrum RPC URL used only to independently verify a withdrawal
# actually landed on Arbitrum (checked after every withdrawal). Defaults to
# the public Arbitrum RPC if left blank.
ARBITRUM_RPC_URL=

########################################
# Required if this app is reachable from anywhere other than your own
# machine (e.g. deployed on a server). Gates every page and action behind
# a login. Pick a real password, not a placeholder — anyone with it has
# full control of every configured account.
########################################

# Password required to use the app. 8+ characters minimum, but use a long,
# random one — this is the only thing standing between the internet and
# your private keys once deployed.
APP_PASSWORD=

# Long random secret used to sign session cookies. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=

# Set to "production" when deployed behind HTTPS (e.g. on EC2 behind Caddy).
# Enables secure (HTTPS-only) session cookies. Leave unset for local
# http://127.0.0.1 development.
NODE_ENV=

# Overrides whether the session cookie requires HTTPS, regardless of
# NODE_ENV. Set to "false" to allow the app to work over plain HTTP (e.g.
# direct IP:port access with no TLS) — only do this if you understand that
# your password and session cookie then travel unencrypted over the
# network, readable by anyone on the same path (shared WiFi, a compromised
# router, etc). Leave blank to use the NODE_ENV-based default.
COOKIE_SECURE=
